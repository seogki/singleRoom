spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/singleroom
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: seogki
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
